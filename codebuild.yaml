version: 0.2

env:
  variables:


phases:
  install:
    commands:

  build:
    commands:
      - echo ===== test =====

  post_build:
    commands:
      - echo CodeBuild Project finished on `date`...
